import React from 'react';
import WidgetError from '../components/WidgetError';

const NotFound = () => {
    return (
        <WidgetError message="We are unable to find a widget at this URL." />
    )
}

export default NotFound;